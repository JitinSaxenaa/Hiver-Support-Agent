# Copy these values into Streamlit Cloud > App settings > Secrets.
# Never commit the real secrets.toml file.

LLM_PROVIDER = "openai"
OPENAI_API_KEY = "paste-a-new-key-here"
OPENAI_MODEL = "gpt-4o-mini"
